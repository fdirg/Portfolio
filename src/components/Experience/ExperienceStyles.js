import styled from "styled-components";
import { ProjectCard } from "../CardProject/CardProjectStyles";
import {
  ProjectsCardsWrapper,
  ProjectsTitle,
  ProjectsWrapper,
} from "../Projects/ProjectsStyles";
import { EducationDescription } from "../Education/EducationStyles";
import { CoursesCard } from "../Courses/CoursesStyles";

export const ExperienceWrapper = styled(ProjectsWrapper)``;

export const ExperienceTitle = styled(ProjectsTitle)``;

export const ExperienceCardsWrapper = styled(ProjectsCardsWrapper)``;

export const ExperienceCard = styled(CoursesCard)``;

export const ExperienceDescription = styled(EducationDescription)``;
